<table class="table table-hover">
    <tr>
        <th>
            {% if current_category == "Loans" %} 
                {{ 'displayexp.receive' | trans({}, 'messages') }} 
            {% else %} 
                {{ 'displayexp.product' | trans({}, 'messages') }}  
            {% endif %}
        </th>
        <th>{{ 'displayexp.price' | trans({}, 'messages') }} </th>
        <th>{{ 'displayexp.date' | trans({}, 'messages') }} </th>
        <th>{{ 'displayexp.description' | trans({}, 'messages') }} </th>
    </tr>
    <tr>